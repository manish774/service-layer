{
  "name": "manish-service-layer",
  "version": "1.0.5",
  "description": "\"Ui service\"",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/manish774/service-layer.git"
  },
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"src/**/*.ts\"",
    "generate": "ts-node ./src/config/initGeneration.ts && npm run format",
    "run": "node/Types.js",
    "create:endpoint": "npm run build && npm run run",
    "build": "npm run generate && tsc"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "prettier": "^3.2.5",
    "ts-node": "^10.9.2"
  }
}

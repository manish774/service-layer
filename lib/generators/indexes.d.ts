import { IServices } from "../models/Services";
export declare class Indexes {
    completeString: string;
    generate(serviceObject: IServices): Promise<string>;
    init(serviceObject: IServices): Promise<string>;
}
export declare class RootIndex {
    completeString: string;
    generate(serviceObject: IServices): Promise<string>;
    init(serviceObject: IServices): Promise<string>;
}
